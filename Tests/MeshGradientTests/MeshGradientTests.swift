import XCTest
@testable import MeshGradient

final class MeshGradientTests: XCTestCase {
    func testExample() throws {
        // This is an example test case to verify the test setup is working
        XCTAssertTrue(true, "Basic test passes")
    }
} 